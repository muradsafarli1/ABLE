// Firebase migration mode: PostgreSQL is no longer required for the normal ABLE deployment.
// The old Render database, if needed for one-time migration, is accessed separately.
console.log('ABLE: PostgreSQL check skipped (Firebase migration mode).');
